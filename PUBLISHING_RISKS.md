# Публикация проекта в открытый доступ: риски и рекомендации

Заметки перед выкладкой репозитория (GitHub и т.п.) и активным продвижением для привлечения потенциальных работодателей.

**Статус пунктов:** ✅ сделано · 🟡 частично · ❌ не сделано

---

## Высокий риск

### 1. Юридический: скрейпинг + недокументированный API

Проект массово парсит [comon.ru](https://www.comon.ru) (Selenium) и обращается к недокументированному endpoint:

`GET https://www.comon.ru/api/v1/strategies/{number}/profit`

Возможные последствия:

- претензии по пользовательскому соглашению (ToS) / несанкционированному доступу к данным;
- при активном распространении чужие запуски увеличат нагрузку на сайт → выше шанс блокировок и внимания владельца сервиса;
- для работодателя это может выглядеть как «кандидат обходит ограничения чужого сервиса».

**Смягчение:**

- 🟡 в README явно указать: личные / образовательные цели, уважать ToS, rate limit, не для коммерции  
  — есть «вежливое использование», «на свой риск», [COMON_SCRAPING_LEGAL.md](COMON_SCRAPING_LEGAL.md); **нет** явных «образовательный / не для коммерции»
- 🟡 API описать как неофициальный, без гарантий, может исчезнуть или измениться  
  — отмечено в [COMON_SCRAPING_LEGAL.md](COMON_SCRAPING_LEGAL.md); в README — кратко, без отдельного абзаца про unofficial API
- ✅ не позиционировать проект как «как обойти Finam / comon»  
  — тон README нейтральный, есть ссылка на правовые заметки

### 2. Инвестиционный дисклеймер

`analiz/rank.py` считает Sharpe, CAGR и ранжирует стратегии.

Без оговорок это воспринимается как инвестиционная рекомендация. Репутационный риск: «по вашему рейтингу потерял деньги».

**Смягчение:**

- ❌ крупный дисклеймер в README: это не инвестиционный совет; данные могут быть неточны; прошлые результаты не гарантируют будущих  
  — такого блока пока нет (есть только «на свой риск» в контексте нагрузки на сервер)

---

## Средний риск

### 3. Утечка инфраструктуры

В коде встречается имя хоста `MEGABAX\SQLEXPRESS` (`lib/save.py`, `alembic.ini`, `SQLDemo.py`, `other/6.py` и др.).

Реальных паролей в репозитории нет (placeholders + Windows Authentication), но имя машины / ПК лишнее. Даже после правки hostname может остаться в git history.

**Смягчение:**

- ❌ вынести строку подключения в `.env` / переменные окружения  
  — `.env` есть только для Chrome (`CHROME_SUPPRESS_BACKGROUND_NOISE`); БД всё ещё в коде
- ❌ в репозитории — только `localhost` или явные placeholders  
  — `MEGABAX\SQLEXPRESS` всё ещё в `lib/save.py`, `alembic.ini` и др.
- ❌ rewrite history / чистый репозиторий  
  — не делалось

### 4. Репутация кода vs портфолио

**Сильные стороны:** Alembic, pytest, метрики риска, CLI, верификация данных.

**Слабые для HR / техскрининга:** хардкод параметров БД, дубли скриптов (`test.py` vs автотесты), каталог `other/`, опечатки в сообщениях коммитов, смешанный стиль.

**Смягчение перед выкладкой:**

- ✅ README: что умеет проект, как запустить, ограничения  
  — есть конвейер, метрики, ссылки на риски / парсинг / вежливое использование
- 🟡 убрать или не публиковать мусорные скрипты  
  — черновики перенесены в `other/`, но каталог и `SQLDemo.py` всё ещё в репо
- 🟡 secrets только через env  
  — `.env` в `.gitignore`, пример для Chrome; **подключение к БД не через env**
- ❌ 1–2 скриншота рейтинга / графика  
  — нет (и `*.png` в `.gitignore`)

### 5. Злоупотребление третьими лицами

Публичный инструмент массовой выгрузки истории: кто-то может гонять его агрессивно; косвенно «ваш» проект.

**Смягчение:**

- ✅ разумные задержки / не перегружать сервер — в README блок «Вежливое использование»
- ✅ не рекламировать параллельный «штурм» comon — прямо сказано не запускать частые и параллельные запросы
- 🟡 формулировка «на свой риск» — есть в README
- ❌ LICENSE (MIT / Apache и т.п.) — файла лицензии нет

---

## Низкий / управляемый риск

| Риск | Статус | Комментарий |
|------|--------|-------------|
| Секреты в `.env` | ✅ | `.gitignore` исключает `.env`; в индексе git файла `.env` нет |
| `reports/*.csv` | ✅ | в ignore: `*.csv`, `*.xlsx` |
| Лицензии зависимостей | ✅ | обычный стек; отдельный аудит не обязателен |
| Персональные данные | ✅ / контроль | карточки публичные; лишние PII не кэшировать |

---

## Как позиционировать для работодателя

**Хорошо:** ETL с внешнего источника → MSSQL → метрики риска → верификация данных → CLI / отчёты.

**Осторожно:** «scraping Finam», «обход API», «лучший рейтинг стратегий для инвестиций».

Предпочтительная формулировка: *personal research / data pipeline / risk metrics on public strategy time series*.

**Статус:** 🟡 частично — README ближе к data/engineering; явной portfolio-формулировки и инвест-дисклеймера ещё нет.

---

## Чеклист перед open source

1. ❌ Убрать / заменить `MEGABAX` и другие локальные имена → env.
2. 🟡 Добавить дисклеймер + пометку про unofficial API и ToS.  
   — есть [COMON_SCRAPING_LEGAL.md](COMON_SCRAPING_LEGAL.md) и ссылки в README; полного инвест-дисклеймера и «unofficial API» в README нет.
3. ❌ Не обещать инвестиционный результат (явный дисклеймер в README).
4. 🟡 Прибрать репозиторий (или отдельная portfolio-ветка без `other/`, `SQLDemo.py`).  
   — скрипты убраны из корня в `other/`, но не исключены из публикации.
5. ✅ Не разгонять проект как «массовый скрейпер comon».  
   — README призывает к вежливому использованию и паузам.
6. ❌ Выбрать LICENSE (MIT / Apache и т.п.) и кратко описать допустимое использование.

---

## Сводка

| Область | Статус |
|---------|--------|
| Вежливое использование / анти-DoS тон | ✅ |
| Заметка про отсутствие явного запрета парсинга | ✅ |
| Документ рисков публикации | ✅ (этот файл) |
| Инвест-дисклеймер | ❌ |
| Убрать hostname `MEGABAX` / DB → env | ❌ |
| LICENSE | ❌ |
| Чистка `other/` / SQLDemo для портфолио | 🟡 |
| Скриншоты | ❌ |

---

## Итог

Технически проект подходит для портфолио, если аккуратно подать его как data / engineering. Главные риски — ToS и недокументированный API Finam/comon, восприятие рейтинга как инвестсовета и сырой код с локальным hostname. При вирусном распространении юридический и репутационный риск растут сильнее, чем польза от «лайков» HR.

Из чеклиста **полностью закрыты** в основном пункты про тон README (вежливое использование, не «штурм» сайта) и документация по парсингу/рискам. **Критично ещё не сделано:** hostname/БД в env, инвест-дисклеймер, LICENSE.
