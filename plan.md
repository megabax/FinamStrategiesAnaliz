# План развития FinamStrategiesAnaliz

Дорожная карта по приоритетам. Детали по рискам публикации — в [PUBLISHING_RISKS.md](PUBLISHING_RISKS.md), по парсингу — в [COMON_SCRAPING_LEGAL.md](COMON_SCRAPING_LEGAL.md).

**Статус:** ✅ сделано · 🟡 частично · ❌ не сделано

---

## Фаза 0. Готовность к открытой публикации (ближайшее)

Цель: безопасный и аккуратный публичный репозиторий для портфолио.

| # | Статус | Задача | Зачем / комментарий |
|---|--------|--------|---------------------|
| 0.1 | ✅ | Вынести строку БД в `.env`, убрать `MEGABAX` из кода и `alembic.ini` | URL берётся из `DATABASE_URL` / `MSSQL_*` (`lib/env.py`); Alembic читает его в `alembic/env.py` |
| 0.2 | ✅ | Инвест-дисклеймер в README | блок «Дисклеймер»: не совет, данные могут быть неточны, прошлое ≠ будущее |
| 0.3 | ✅ | Явно описать Selenium + **неофициальный** API profit | раздел «Источники данных»; `--force-selenium`; не Trade API |
| 0.4 | ✅ | Добавить `LICENSE` | MIT, ссылка в README |
| 0.5 | ✅ | `other/` и черновики локально, не в репо | удалены из git; `other/*` в `.gitignore` |
| 0.6 | ✅ | 1–2 скриншота рейтинга / графика | `docs/` + раздел в README; `!docs/*.png` в `.gitignore` |

---

## Фаза 1. Данные и загрузка

Цель: полная и устойчивая история по стратегиям без лишней нагрузки на comon.

| # | Статус | Задача | Зачем / комментарий |
|---|--------|--------|---------------------|
| 1.1 | ✅ | Догрузить историю по всем активным стратегиям | код умеет массовую загрузку (`load_history.py`); полный охват ~600 стратегий в БД — операционная задача, не закрыта |
| 1.2 | ❌ | Настраиваемый rate limit / паузы в API и Selenium | в Selenium есть фиксированные `sleep`; отдельного конфига пауз/лимитов нет (только текст в README) |
| 1.3 | ✅ | Fallback API → Selenium | `probe_profit_api` + `load_strategy_history_auto`; при сбое API — Selenium |

---

## Фаза 2. Анализ и отчёты

Цель: удобная оценка привлекательности стратегий.

| # | Статус | Задача | Зачем / комментарий |
|---|--------|--------|---------------------|
| 2.1 | ❌ | Взвешенный **score** в `rank.py` | есть сортировка по Sharpe/Calmar/…, единой формулы score нет |
| 2.2 | 🟡 | Equity curve + drawdown chart | `stathist.py` с `-n`: дневной %, MA, ±std; **не** equity/drawdown |
| 2.3 | ❌ | Сравнение нескольких стратегий на одном графике | нет |
| 2.4 | ❌ | Сверка `cagr_pct` vs `annual_income` | обе колонки есть в CSV рейтинга, отдельной сверки/флага расхождения нет |
| 2.5 | 🟡 | Окна 1Y / 3Y / YTD одной командой | есть `--from-date` / `--to-date`; пресетов (`--window 1y`) нет |
| 2.6 | 🟡 | Симулятор входов/выходов (long-only) | OOP в `sim/`; PnL есть; `MaStdThresholdAlgorithm` + `python -m sim.backtest`; мультистратегия/комиссии — позже |
| 2.7 | 🟡 | Прогноз доходности (`lib/nnlib.py` / LSTM) | заготовка LSTM есть; нужна интеграция в анализ/отчёты (CLI, график vs факт) |

---

## Фаза 3. Качество кода и тесты

Цель: проект выглядит и ведёт себя как продуктовый инструмент.

| # | Статус | Задача | Зачем / комментарий |
|---|--------|--------|---------------------|
| 3.1 | ❌ | `requirements.txt` / `pyproject.toml` | README упоминает requirements «если есть»; файла нет |
| 3.2 | 🟡 | Расширить pytest | есть тесты CSV, comon API, archive-check; мало/нет по metrics и границам загрузки |
| 3.3 | 🟡 | Развести `test.py` и автотесты | автотесты в `tests/` + `pytest.ini`; скрипт сверки всё ещё называется `test.py` |
| 3.4 | 🟡 | Убрать дублирование (даты, CSV, драйвер) | частично: `csv_export`, `browser`, `verify`, `comon_api`; парсинг дат и т.п. ещё дублируется |
| 3.5 | ❌ | CI (GitHub Actions): pytest | нет `.github/workflows` |

---

## Фаза 4. Позже / по желанию

| # | Статус | Задача | Комментарий |
|---|--------|--------|-------------|
| 4.1 | ❌ | UI (Streamlit/Dash) | нет |
| 4.2 | ❌ | Экспорт рейтинга в Excel | нет (CSV есть) |
| 4.3 | ❌ | Несколько воркеров загрузки на VPS | нет |
| 4.4 | ❌ | Bulk-import истории (CSV/Parquet → MSSQL) | низкий приоритет: Finam файлы не отдаёт; перенос машины = бэкап БД; имеет смысл только при смене СУБД / шаринге сэмпла |
| 4.5 | ❌ | Мониторинг «API сломался» (алерты) | fallback уже есть; постоянная проверка/уведомления нужны в основном при cron на VPS |
| 4.6 | 🟡 | Согласовать границы периодов API / Selenium / verify | сдвиг API↔БД и clamp/retry в verify уже есть; закрывать при реальном баге на краях периода (контракт дат + pytest) |

---

## Сводка по фазам

| Фаза | ✅ | 🟡 | ❌ |
|------|----|----|----|
| 0. Публикация | 6 | 0 | 0 |
| 1. Данные | 2 | 0 | 1 |
| 2. Анализ | 0 | 4 | 3 |
| 3. Качество | 0 | 3 | 2 |
| 4. Позже | 0 | 1 | 5 |

Ближе всего к «готово» по данным: fallback API→Selenium (**1.3**), документация/тон README (см. PUBLISHING_RISKS), базовый рейтинг, `stathist -n` и симулятор `sim/` как задел к 2.2 / 2.6.

---

## Приоритет на ближайшие итерации

1. **Фаза 2.7** — прогноз доходности (довести `nnlib` / LSTM до CLI и отчёта).  
2. **Фаза 2.1–2.2** — score и equity/drawdown.  
3. **Фаза 1.2** — rate limit / паузы при массовой загрузке.  
4. Остальное — по мере необходимости.

---

## Вне скоупа (намеренно)

- коммерческий сервис «лучшие стратегии Finam»;
- агрессивный параллельный скрейпинг;
- позиционирование рейтинга как инвестиционной рекомендации.
